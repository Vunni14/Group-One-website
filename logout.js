function logout() {
    // Perform logout actions (e.g., clearing session, redirecting to login page)
    // Example: Redirecting to the login page (adjust URL as needed)
    window.location.href = "index.html"; // Replace with your login page URL
}
